// the app's base path, used when doing manual window.location navigation
// (Next's own <Link> and router already apply this automatically)
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH || "/Cod";
